# Scanner Fixtures

This folder intentionally does not contain a raw EICAR file because many host antivirus tools will quarantine it immediately.

The scanner tests build the canonical EICAR string from chunks only when needed, and the raw-file EICAR test is ignored by default.
